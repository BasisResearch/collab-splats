# Keep the Docker build context (and image) lean. COPY . pulls the repo into the
# builder + runtime stages; exclude history, local outputs, caches, and local docs.
.git
.gitignore
.worktrees/
**/__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/
*.egg-info/
.mypy_cache/
.ruff_cache/
evals/results/
docs/superpowers/
.venv/
