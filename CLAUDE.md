# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Always follow:
- Use rtk tools /workspace/.claude/RTK.md
- At the start of every conversation always call /brainstorming -- use superpowers to accomplish tasks.
- Active spec/plan for in-flight work lives in docs/superpowers/specs/ and docs/superpowers/plans/.
- Architecture decisions: docs/superpowers/decisions/NNN-slug.md (sequential numbering).
- User-facing module docs live in docs/ (mirrors code tree).
- Superpowers specs/plans belong in docs/superpowers/specs/ and docs/superpowers/plans/.

## In-Flight Work

These tasks are started but not complete — do not assume their targets are done:

- **gt-eval-harness** — ground-truth ATE evaluation harness ([spec](docs/superpowers/specs/2026-05-07-gt-eval-harness-design.md) · [plan](docs/superpowers/plans/2026-05-07-gt-eval-harness.md))
- **feedforward-import-cleanup** — import hygiene pass ([spec](docs/superpowers/specs/2026-05-08-feedforward-import-cleanup-design.md) · [plan](docs/superpowers/plans/2026-05-08-feedforward-import-cleanup.md))
- **feedforward-mesh** — feedforward → TSDF mesh pipeline ([spec](docs/superpowers/specs/2026-05-14-feedforward-mesh-design.md) · [plan](docs/superpowers/plans/2026-05-14-feedforward-mesh.md))
- **docs-site** — Sphinx site setup ([spec](docs/superpowers/specs/2026-05-20-docs-site-design.md) · [plan](docs/superpowers/plans/2026-05-20-docs-site.md))
- **bae-vggt-parity** — verify BA matches upstream `zitongzhan/vggt --implementation bae` ([spec](docs/superpowers/specs/2026-05-20-bae-vggt-parity-design.md))
- **loma-matcher** — LoMa local matcher for localization ([spec](docs/superpowers/specs/2026-07-08-loma-matcher-integration-design.md) · [plan](docs/superpowers/plans/2026-07-08-loma-matcher-integration.md))

Recently completed (2026-08-23): **instantsfm-backend** — classical global SfM as a second `pointcloud.method`, plus a hard artifact rename. **`feedforward.zarr` → `pointcloud.zarr` repo-wide, no fallback, no resolver** — pre-rename scenes need a one-time `mv` (local) / `rclone moveto` (processed bucket) or a re-run (`configs/README.md` → "Processed scene layout"). `FeedforwardResult.save_zarr(..., extra_attrs=...)` stamps provenance attrs `method`, `backend` (+ `instantsfm_version` via `importlib.metadata`); `confidence`/`mv_*` are **absent, never zeros** when the method doesn't produce them and mesh/`lift_features`/splats-depth handle absence (unmasked / uniform weights). Config surface: `pointcloud.method: sfm`, `pointcloud.backend: instantsfm`, `pointcloud.instantsfm.features: colmap` (`_INSTANTSFM_FEATURES`, the only value — upstream v0.3.0 ignores the handler name and always runs CPU SIFT + exhaustive). `validate_config` raises `ValueError` for `bundle_adjustment: true`, loop closure, or an unknown `features` with sfm; `refine_poses` / `--stages refine` refuse sfm. New `collab_splats/pointcloud/sfm.py` (`InstantSfMCreator` over the upstream python API `ReadData → GenerateDatabase → ReadColmapDatabase → Config → ReadDepthsIntoFeatures → SolveGlobalMapper → WriteGlomapReconstruction`; `generate_vda_depth` / `vda_depth_complete` for Video-Depth-Anything metric depth, imported from the `third_party/Video-Depth-Anything` clone root via sys.path). `Reconstructor._run_sfm`: stage `frames.zarr` → `<scene>/instantsfm/images/frame_NNNNNN.jpg` → VDA depth `depth_vda/images/npy/<stem>.npy` at 518-wide model res (skipped when the stem set is complete) → InstantSfM (`colmap/instantsfm.db` + `colmap/sparse/0`) → COLMAP image names renamed to stems → `pointcloud.zarr` (depth/images/poses/K at model res, `world_points` unprojected from VDA depth) → `sparse_pc.ply` + `transforms.json`. `colmap/instantsfm.db` is its own `PUSH_EXCLUDES` entry (`/*/colmap/instantsfm.db`) beside verify's `database.db`. Install via `setup.sh`: `instantsfm` pinned git commit `--no-deps` (upstream pins numpy 1.26.4), `pyceres==2.3 scikit-sparse==0.4.15 easydict==1.13`, VDA clone at `4f5ae23` + best-effort ~1.5 GB `metric_video_depth_anything_vitl.pth`; system `colmap` binary on PATH; **plain `uv sync` prunes the `--no-deps` packages**. Licences: InstantSfM CC-BY-NC-4.0 (research use cleared), VDA code Apache-2.0 / weights CC-BY-NC-4.0. Traps: **instantsfm's single-camera depth path reads only `depth_vda/images/npy/<stem>.npy`** (per-frame npy, not a stacked array); **upstream `GenerateDatabase` swallows `CalledProcessError` AND lets colmap spawn one CPU-SIFT thread per HOST core (96) — OOM-killed under the 46.6 GB cgroup cap**, so `sfm.py` reimplements it (`_generate_sift_database`, attributed, `check=True`; GPU SIFT when CUDA is available — measured 100×1920×1080: extract 7 s vs 90 s, match 55 s vs ~816 s — else CPU `num_threads 8`) and gates DB reuse on keypoints+two_view_geometries counts (`_sift_database_valid`), never bare existence; **COLMAP image names must be stems** (`frame_NNNNNN`, the feedforward contract) or every frame-idx join downstream breaks; **`SolveGlobalMapper` raises a bare `IndexError` under numpy 2 when tracks filter to 0** (re-raised with an actionable message); **upstream packs `(image_id << 32) | feature_idx` track ids into an int32 `Tracks.ids` array — numpy 1.x wrapped silently (collision risk), numpy 2 raises `OverflowError` for any track rooted past image 0**, fixed by `_patch_instantsfm_track_ids` (monkey-patches `FindTracksForProblem` to renumber ids sequentially; ids are opaque labels downstream); **instantsfm's global positioning/BA drive `bae.optim.LM`, hitting the same bae-0.2.4 × pypose-0.7.5 `RobustModel.forward(target)` TypeError ba-pipeline-wiring fixed** — instantsfm builds its optimizers internally, so `_patch_pypose_robustmodel_target` defaults `target=None` class-level (vs the per-instance bind in `geometry/bundle_adjustment.py`); **`original_coords` must be ORIGINAL pixels** (`[0,0,W,H,W,H]`), not model res. Known limitation: dashboard `_ensure_lift_inputs` treats a `pointcloud.zarr` without `confidence` as legacy and re-pulls dense members for every instantsfm scene (not changed). Measured numbers (runtime, ATE vs the feedforward backends): **owed (Task 9 probes)**; `colmap`/`hloc` validate (`ColmapCreator`/`HlocCreator` exist in sfm.py) but are not wired into `_run_sfm` (`NotImplementedError`) ([plan](docs/superpowers/plans/2026-08-23-instantsfm-backend.md)).

Recently completed (2026-08-22): **preproc-cleanup** — `collab_splats/preproc` is now two steps, **measure then select**. Closes out the **video-quality-report** pass, which shipped 2026-08-21 (`compute_video_quality` + the split into video/qa/sampling) and is no longer in flight. `qa.compute_video_quality(video, workers=N)` writes `video_quality_report.json` beside `frames.zarr` (per-frame photometry + per-pair motion, **report-only: no thresholds, no verdicts**); the three samplers `sample_fps` / `sample_uniform` / `sample_optical_flow` read that report through `sampling.filter_frame_quality`, which is where every threshold lives. **The `sample_frames(method=...)` dispatcher is gone** — with it `score_frames`, `check_frame_quality`, `is_stale`, `_combine_scores`, `_sample_positions`, `_iter_scored_frames` and the module-level threshold constants; `sampling.py` 871 → 470 lines. `preproc/__init__.py.__all__` is 11 names and `tests/preproc/test_sampling.py::test_public_api_surface` asserts **exact set equality**, so a re-export cannot drift back in. `Reconstructor._extract_frames` → public **`extract_frames`** (measures, dispatches, refuses an empty selection, writes `frames.zarr`). Config: `preproc.n_workers` (report only; NOT auto-derived — `os.cpu_count()` reports HOST cores inside this container and ignores the cgroup quota). Exposure is now a 256-bin `cv2.calcHist`, ORB detect is split from match and cached per frame. **Production bug found on the way: `evals/datasets.py::_load_video` passed `fps=` alongside `method="uniform"`, which `sample_frames` rejects — every video-dataset eval load raised unconditionally.** **Measured on the 2388-frame tutorial video (96-core host): report 180.1 s serial → 93.9 s at 4 workers (1.92×), byte-identical reports across worker counts; end-to-end `extract_frames` 98.3 s cold, 28.7 s when the report already exists; ORB split 1.34× over 400 frames.** Traps: **the per-worker thread pin is load-bearing — unpinned, 4 workers measured 0.67×, slower than serial** (`cv2.setNumThreads(1)` + `OMP_NUM_THREADS=1` + `OPENBLAS_NUM_THREADS=1`), and **ffmpeg's own decode threads are still unpinned**, the leading (unverified) reason 1.92× falls short of the harness's 3.04× prediction; input seek (`-ss` before `-i`) can land off-by-N on broken timestamps, so the merge asserts **integer frame-index contiguity** and names `workers=1` as the fix; histogram exposure only equals the numpy path with `.astype(np.float64)` and a two-order-statistic median; **blur reads the 480-wide analysis gray but exposure must read the NATIVE gray** — downscaling averages scattered saturated pixels away and clipping fractions read 0.0 on a blown capture. Owed: `keyframe_extraction.ipynb` rebuild (uses `score_frames` and the deleted gate), BFMatcher `nfeatures` tuning (changes published values, out of scope for a cleanup), a second parity video near the `exposure_std >= 10` boundary ([spec](docs/superpowers/specs/2026-08-22-preproc-cleanup-design.md) · [plan](docs/superpowers/plans/2026-08-22-preproc-cleanup.md)).

Recently completed (2026-08-22): **splats-module** — `collab_splats/nerfstudio/` + `wrapper/splatter.py` (gsplat-rade fork) retired for `collab_splats/splats/`, a minimal trainer on upstream gsplat pinned `d2f5c0f` (v1.5.3): `cameras.py` (`CameraOptModule`), `losses.py` (`OPTIONAL_LOSSES` registry: depth, normal_consistency, distortion, opacity_reg, scale_reg), `rendering.py` (`render_view` — 3dgs via `rasterization` + `extra_signals` per-Gaussian normals, 2dgs via `rasterization_2dgs`), `trainer.py`, `outputs.py`. Wired as leaf stage `splats` (`Reconstructor.splats()`, `_STAGE_DEPS["splats"]==["pointcloud"]`, config block `splats:` in base.yaml, `enabled: false`); outputs `<backend>/splats/{splats.ply, ckpt.pt, splats.zarr, splats_quality_report.json}`, all in the COLMAP world frame. Densification is primitive-bound: `MCMCStrategy(cap_max)` for 3dgs (needs `opacity_reg`+`scale_reg`), `DefaultStrategy(grow_grad2d)` + distortion for 2dgs — no user-facing `strategy:` key. **Spec deviation: depth targets are model-res `pointcloud.zarr` (then `feedforward.zarr`) depth (aligned to `image_paths` by frame idx, masked by `mesh.conf_percentile`, 0 = no target, nearest-resized in the trainer), not COLMAP points3D/tracks** — too sparse for a per-pixel disparity L1. `mesh.source: splats` deferred to a follow-on; 3DGS `extra_signals` normal quality is unmeasured (owed). PAGaS seam = `render_view` + one `OPTIONAL_LOSSES` entry. Traps: **gsplat `rasterization_2dgs` returns WORLD-frame normals** (python einsum after the kernel, invisible from the kernel) so `render_view`'s w2c rotate-back is correct; **the kernel rejects 7 colour channels** → normals zero-padded to 4 extra; **trainer loop order is optimizer.step → pose opt → schedulers → `strategy.step_post_backward`** (upstream simple_trainer) — reversing it silently skips refinement. (spec-era owed list superseded by the follow-ups below) ([spec](docs/superpowers/specs/2026-08-22-splats-module-design.md) · [plan](docs/superpowers/plans/2026-08-22-splats-module.md)). **Follow-ups (2026-08-23):** `mesh.source: splats` built (TSDF over `splats.zarr` depth/rgb, alpha as confidence; default stays `feedforward` — splat meshes carry 2.3–2.7× more speckle components); `evals/scripts/eval_splats.py` (native-res inputs from `frames.zarr` + K rescaled from model res; `--model-res` opt-out) and `analyze_splats.py` (normals sanity + mesh comparison); tutorials `03_splats/train_splats.ipynb` + `06_mesh/splats_mesh.ipynb` rebuilt. **Measured on `data/tutorial/` (30 frames, 1920×1080, 30k steps): 3dgs psnr 19.74 ssim 0.707 @ 57 ms/step; 2dgs 19.96/0.707 @ 60 ms/step; pose_opt +0.44 dB for +5% time (and **+2.2 dB on the 300-frame GoPro scene — `splats.pose_opt` now defaults `true`**, Table 4 of the report); 3DGS `extra_signals` normals unit/no-flip but ~21° median off the depth normal.** Two production bugs: **`distortion: {weight: 100.0}` shipped where upstream uses `1e-2` — 2DGS pruned to ~140k gaussians, PSNR 13.5** (now 0.01 in base.yaml/trainer/spec); **`fused_ssim` was missing** so gsplat fell back to torch SSIM, and 3DGS normals rendered every step before `normal_consistency.start` — fixed (`render_normals` gated on `loss_active`), 1.8× faster pre-7000, normals cost −35% once on. Deps: `fused-ssim` git pin `a7c48d6` (needs nvcc, `no-build-isolation-package`). Traps: PSNR across resolutions is NOT comparable; `CUDA_VISIBLE_DEVICES=""` breaks importing `collab_splats.mesh` (vggt CUDA warm-up at import). Still owed: Docker rebuild (no docker binary here), holdout/LPIPS eval, `05_semantics` `FeatureAutoencoder.load(path)` one-arg calls stale (signature `(path, extractor)`) ([report](docs/superpowers/specs/2026-08-23-splats-measured-report.md) · [plan](docs/superpowers/plans/2026-08-23-splats-followups.md)).

Recently completed (2026-08-23): **video-quality-plots** — `video_quality_report.json` rendered to two static PNGs beside `frames.zarr` for the data-dashboard (`photometric.png`: blur/laplacian/exposure/clipping; `motion.png`: translation/parallax/matches — merged from five single-metric files on 2026-08-23, parallax pinned to [0,1]). Two stacked-panel plotters in `collab_splats/preproc/viz.py` (`plot_photometric` / `plot_motion(report, out_dir, *, selected=None) -> Path`), matplotlib only, raw columns only (report-only rule inherited), x = seconds, `selected` frames marked by faint green `vlines` (axvspan one frame wide is <1 px on long videos). Wired into `Reconstructor.extract_frames` video branch after `FrameStore.create` — written whenever frames are, **no backfill**: scenes processed earlier get PNGs only on `preprocess(overwrite=True)`. `reconstructor.py` now imports matplotlib; dashboard fast-bind stays clean because `remote/__init__.py` never imports it. Follow-ups the same day: each timeseries panel carries a flush seaborn marginal histogram (`_timeseries_grid`), the translation cumsum panel is gone, **`plot_motion` and `plot_correlation` return `None` and write nothing when the report has no samples**; `plot_correlation` clamps joint axes to the data range; plus `plot_frame_extremes(report, video_path, out_dir, column=...)` (top/bottom-n frame montage, dpi 150) and `plot_correlation(report, x, y, out_dir)` (seaborn `JointGrid` (regplot joint, lightblue/darkblue-outline points, blue fit, filled KDE marginals, `white` style), Pearson/Spearman text, per-frame columns aligned to pairs by `frame_idx_a`; seaborn is now a dependency). Rejected by design: helper layer beyond the grid builder, registry tuple, availability check ([spec](docs/superpowers/specs/2026-08-23-video-quality-plots-design.md) · [plan](docs/superpowers/plans/2026-08-23-video-quality-plots.md)).

Recently completed (2026-08-21): **matcher-feature-cache** — verify()'s pair loop no longer re-runs per-image feature extraction per pair. LoMa's forward is split (`_split_loma_forward`): `extract()` runs the detector half once per image and persists `keypoints_normalized` (pre-transform detector coords) into the zarr localization DB (`save_index`, all-or-none payload); `match()` replays the pair half from stored tables. New allowlist `FEATURE_MATCH_MODELS = {"xfeat", "loma"}` (localization/extractors.py) gates the 2-arg feature-level `match(query, db)`; membership is licensed ONLY by the CUDA parity tests in `tests/localization/test_local_matcher.py` (byte-equal matches vs pairwise), no runtime probes. Verification dispatch: `pairwise = isinstance(matcher, LocalMatcher) and model_name not in FEATURE_MATCH_MODELS`; the feature-level branch needs no images. `Reconstructor.verify()` rebuilds the DB ONCE when the cache predates the payload, then reloads. `verification.json` now carries `summary["phase_seconds"]` (db_export, pair_matching, db_match_writes, verify_matches, triangulate) + cold-start `verify(): X took Ns` log lines. **Production bug found by the evidence run: `build_localization_db(overwrite=True)` was a silent no-op** — `from_feedforward` always cache-hits on an existing `local_features/<extractor>/reconstruction` group; fix deletes the stale group first (regression tests `tests/wrapper/test_localization_db_overwrite.py`). **Measured Goprosplat 300 frames vggt_omega+loma: 44.1 ms/pair vs 1033.5 pairwise (23×); verify() 546.7 s vs 2511.6 s (4.6× incl. one-time 196 s rebuild, ≈350 s steady ≈7.2×); pycolmap C++ now dominates the residual (verify_matches 139.3 s + triangulate 29.3 s) — further speedup belongs to a pycolmap-native migration, deliberately stopped.** Traps: **mapanything flips TF32 on at module import** (via feedforward.base) — parity gates fail under TF32 (near-threshold matches flip, 969 vs 967), fixed with a `strict_fp32` fixture (docs/known-test-failures.md); `sys.modules[type(m).__module__]` reach-in is env-pinned, parity tests are the only tripwire ([plan](docs/superpowers/plans/2026-08-20-matcher-feature-cache.md) · [measured](docs/superpowers/specs/2026-08-20-scene-error-report-measured.md)).

Recently completed (2026-08-19): **ba-track-quality-parity** — follow-on that fixed the BA regression recorded below. Audit against upstream `zitongzhan/vggt demo_colmap.py` found our LM core *identical* (same `LM` + `StopOnPlateau(steps=40, patience=3, decreasing=1e-3)`, `reject=10`, `TrustRegion(up=2.0, down=0.5**4)`, `PCG()`, run **once** — upstream's own `# TODO: add iterative BA` means single-shot filtering is upstream behaviour, not our deviation); the divergence was entirely **track quality + observation filtering**. `BundleAdjustmentConfig` defaults changed: NEW `vis_thresh=0.2` (old `astype(bool)` admitted any score>0), NEW `fine_tracking=True` (was hardcoded False — coarse tracks ~1–2 px vs fine ~0.3–0.5 px), `max_query_pts` 2048→4096, `query_frame_num` 5→8, `shared_camera` False→True (per-frame focal spread is model noise; omega 11% fx). New `_filter_observations` applies upstream's filter ORDER (drop under-inlier frames BEFORE the ≥2-obs landmark check — our old order kept depth-unconstrained single-obs landmarks). `fine_tracking` joins the track cache key (`vis_thresh`/`shared_camera` deliberately excluded so ablations reuse the cache); **all pre-existing on-disk track caches invalidate once**. Config surface unchanged — dataclass defaults only, `pointcloud.bundle_adjustment` stays one boolean. **Measured chess/seq-01 100 frames (baselines reproduce the old sweep exactly): mapanything ATE 0.0133→0.0089 (−32.7%, was −9.8%; ATE max 44.8→17.2 mm), vggtx 0.00819→0.00884 (+8.0%, was +72%). BA is no longer harmful on sub-cm baselines but costs 5–6× runtime, so the default stays `bundle_adjustment: false`.** Traps: rotation regresses slightly on both backbones while translation improves (shared-focal removal appears absorbed into rotation — `ba_percam` ablation condition added to `evals/scripts/eval.py` to isolate it); **AUC@5 is ill-conditioned on this sequence** (10–20 mm inter-frame baselines make the bearing term swing past the 1° bins on sub-mm shifts — prefer ATE/RPE indoors); track extraction is effectively deterministic (ALIKED top-K precedes the unseeded `randperm` at `track_predict.py:173`), so single-run numbers are not sampling noise. Owed: `ba_percam` attribution result, then option B (1024/original-res track extraction) and iterative BA ([spec](docs/superpowers/specs/2026-08-19-ba-track-quality-parity-design.md) · [plan](docs/superpowers/plans/2026-08-19-ba-track-quality-parity.md)).

Recently completed (2026-08-19): **ba-pipeline-wiring** — LM bundle adjustment wired into the pipeline as leaf stage `refine` (`_STAGE_DEPS["refine"]==["pointcloud"]`): ONE method `Reconstructor.refine_poses(overwrite)` behind two triggers — inline (`pointcloud.bundle_adjustment: true` appends refine after pointcloud) and disk (`--stages refine` from `environments-processed`, zero rerun.py changes). Loads `FeedforwardResult.load_zarr(load_images=True)`, runs `BundleAdjustment.refine().reproject()`, rewrites COLMAP (`build_pycolmap_reconstruction` → rescale → `write_binary`), zarr poses + recomputed `world_points`, `sparse_pc.ply`, `transforms.json`; marker+provenance `colmap/refine.json`. LC×BA mutually exclusive (ValueError). K-space guard in `_scale_intrinsics_to_model` (2·cx vs W_model vs crop centre) — all backends now emit model-res K, unguarded scaling double-applied the crop transform. Refine does NOT invalidate downstream outputs (documented contract, configs/README.md). Two latent production bugs fixed by the first live BA run since the env upgrade: bae 0.2.4 `LM.step` vs pypose 0.7.5 `RobustModel.forward(target)` TypeError (BA optimize was dead in production; 3 test xfails removed) and LoGeR float32 rotations failing `mat2SE3` orthogonality (SVD projection). Explicit BA progress logging (per-LM-step loss both paths). **Measured chess/seq-01 ×4 backends: BA helps only the weakest baseline (mapanything −10% ATE, all metrics better) and hurts sub-cm baselines (vggtx/omega/loger ATE +28–72%) — default stays `bundle_adjustment: false`; BA is for visibly-bad baselines, not a free upgrade.** **(Verdict SUPERSEDED by ba-track-quality-parity below — those numbers were measured with pre-parity track quality.)** Windowed BA (`--submap_size`) still broken (LC images gap, out of scope) ([spec](docs/superpowers/specs/2026-08-18-ba-pipeline-wiring-design.md) · [plan](docs/superpowers/plans/2026-08-18-ba-pipeline-wiring.md)).

Recently completed (2026-08-17): **geometric-verification** — self-diagnosis for feedforward reconstructions: does the model's geometry survive classical epipolar/triangulation checks? New `collab_splats/geometry/verification.py`: `verify_reconstruction(recon, features, matcher, output_dir)` exports keypoints+matches to a COLMAP `database.db`, runs Tier 1 (`pycolmap.verify_matches` with `compute_relative_pose=True` → per-pair `PairStats` rot/t-direction error vs the model's relative pose) and Tier 2 (`pycolmap.triangulate_points` from known poses → per-frame track survival + reprojection stats), writes `colmap/verification.json` (all distributions median/p90/p99, nan→null) and `colmap/verified/` model. **Report-only contract: nothing feeds back into the reconstruction — and `triangulate_points` MUTATES its reconstruction arg in place (returns the same object), so the entry point copies via `pycolmap.Reconstruction(recon)` first.** Wired as a leaf stage `verify` (`_STAGE_DEPS["verify"]==["pointcloud"]`, so it re-runs from `environments-processed`); config surface is ONE boolean `pointcloud.geometric_verification: false`; features shared with localization via the zarr cache (`build_localization_db` + `load_reconstruction_features`); XFeatStar rejected by design (no keypoint table indices). `database.db` excluded from GCS push (anchored `/*/colmap/database.db`). Sequential pairs only — loop pairs are a follow-on. Negative control proven: +2° on one pose → exactly that camera's pairs flagged (~2° error) and its track survival collapses, clean pairs <0.2°. `evals/scripts/eval_verification.py` compares Tier 1/2 against 7-Scenes GT (tmux only). **Owed: the chess/seq-01 experiment (plan Task 8 Step 4: xfeat, loma, `--perturb_deg 2.0` control) — compute, human-gated; results append to the measured report** ([spec](docs/superpowers/specs/2026-08-14-geometric-verification-design.md) · [plan](docs/superpowers/plans/2026-08-14-geometric-verification.md)).

Recently completed (2026-08-13): **multiview-confidence-parity** — one geometric cross-view depth-consistency filter shared by every feedforward backbone. `compute_multiview_depth_confidence` (pointcloud/feedforward/base.py) now returns a `MultiviewConfidence` dataclass (`ratio`, `inlier_count`, `valid_count`, `judged`); `multiview_mask(mv, valid_depth, min_views=K)` is the only thresholding path. **`mv_conf_threshold` is gone from all four creators** — replaced by `min_views`, an inlier *count*, because the ratio is quantized with a huge atom at 1.0 and any percentile threshold on it collapses. `min_views=1` is provably identical to the old `threshold=0.0`, so MapAnything's shipping output is unchanged. Upstream *substitutes* mv for the learned confidence; **we AND them** (strictly more conservative). Sampling is nearest, not bilinear (bilinear across a depth discontinuity fabricates depth on no surface); occluded views leave the denominator while free-space violations stay as outliers; views with no overlapping partner keep all their pixels. New guard: the principal point must lie strictly inside the depth grid, which catches the model-res-depth-vs-original-res-K class that caused the 2026-08-11 mesh regression. `mv_ratio`/`mv_inlier_count`/`mv_valid_count` persist to `pointcloud.zarr` (then `feedforward.zarr`) when computed and are **absent, never zeros, when not** — **existing zarr stores have no `mv_*` arrays and are not backfilled**. Config surface is ONE boolean, `pointcloud.use_multiview_confidence`; tolerances stay creator fields. **Measured on 7-Scenes chess/seq-01 (60 frames, Step D of the report): mv never beat the learned confidence percentile at comparable retention, so the default stays `false` on base.yaml and on every non-metric creator.** Best case (omega, rel=0.01 K=16) cut the >10%-error pixel fraction 27% for 10% of pixels — real, ~2.6× more pixel-efficient than the learned filter, but small; the shipped rel=0.05/K=1 was *inert* (99.9% retention), hence recalibrated to **rel=0.01, K=2** on vggtx/omega/loger. MapAnything left at rel=0.02/abs=0.02/K=1 — it gains least (out10 −4% even at K=16) and parity is worth more. **`min_views` is an absolute count: K > N−1 would empty every judged view, so `multiview_mask` clamps per pixel (`required = min(min_views, valid_count)`) — an unreachable K degrades to "every partner you have must agree", never to "delete". K=1 is unchanged by the clamp, so MapAnything stays byte-identical; the corollary is that the K≥8 sweep rows are upper bounds, since on a short scene the clamp weakens them to "all agree".** VGGT-SPARK verified to inherit the path in a fresh subprocess; not wired into `creator_map`. Owed: sweep on a second scene/dataset, and downstream (cloud/mesh/ATE) effect is unmeasured ([spec](docs/superpowers/specs/2026-08-13-multiview-confidence-parity-design.md) · [report](docs/superpowers/specs/2026-08-13-multiview-confidence-measured-report.md) · [plan](docs/superpowers/plans/2026-08-13-multiview-confidence-parity.md)).

Recently completed (2026-08-13): **fps-frame-sampling** — `frame_selection` gained an `fps` method that samples at a constant wall-clock rate (`_sample_fps` + `_fps_targets`), joining `uniform`/`optical_flow` as a third sibling over a shared `_sample_positions` body (single ffmpeg `select=` pass — the 14fdc42 fast path is untouched). `max_frames` now has ONE meaning everywhere — the frame budget: the target COUNT for `uniform`, a ceiling for `fps`/`optical_flow`. `min_frames` is an `fps`-only floor; outside `[min_frames, max_frames]` fps targets are **re-spread over the whole video, never truncated** (the old `targets[:max_frames]` dropped the tail). `frame_proportion` **deleted** — it controlled no physical quantity. Passing another method's knob raises `ValueError`. `fps` joined `FrameStore._STALENESS_KEYS`, else changing the rate at fixed `max_frames` silently reuses the old `frames.zarr`. base.yaml defaults: `frame_selection: fps`, `fps: 1.0`, `min_frames: null`, `max_frames: 300` — verified on `data/tutorial/` (2388 frames @23.98fps → 100 frames spanning 0–2373). **Owed: LC calibrations and the 4-backbone ATE numbers were measured under `uniform` spacing; `loop_closure.yaml` now uses `fps: 4.0` + `min_frames: 300` and the numbers need re-verification.** `splatter.py` keeps its own unrelated `frame_proportion`/`Literal["fps", ...]` on the nerfstudio path — deliberately untouched, names now collide ([spec](docs/superpowers/specs/2026-08-13-fps-frame-sampling-design.md) · [plan](docs/superpowers/plans/2026-08-13-fps-frame-sampling.md)).

Recently completed (2026-08-11): **mesh-tsdf-adapter-convergence** — one TSDF adapter. `Reconstructor._run_tsdf_mesh` fused model-resolution depth from `pointcloud.zarr` (then `feedforward.zarr`) against original-resolution COLMAP intrinsics (`build_colmap` rescales the camera) and divided already-`[0,1]` RGB by 255 — measured 5.06M → 75k vertices and black vertex colours on `data/outputs/`. Both lines dated to 6bc9c81 and were invisible while `mesh.enabled: false`. `_feedforward_to_tsdf_inputs` (`mesh/utils.py`) now reads `result.depth`/`result.images` directly (world_points projection and PIL re-read/crop/`/255` deleted as measured-redundant) and is the single path for both `Reconstructor` and the dashboard. COLMAP stays the pose authority; guards on frame-count mismatch and on `[0, 255]` RGB. Post-fix baseline on `data/outputs/`: 5,060,214 verts at `depth_trunc=20`, 711,079 at the shipping `depth_trunc: 2.0` — **`depth_trunc` is now the dominant limit on mesh extent (86% of vertices), and depth is non-metric so `2.0` is not "2 metres"**. **Every `mesh.ply` on disk — local and under `environments-processed/` — was fused with the wrong K and needs `mesh(overwrite=True)`** ([spec](docs/superpowers/specs/2026-08-11-mesh-tsdf-adapter-convergence-design.md) · [plan](docs/superpowers/plans/2026-08-11-mesh-tsdf-adapter-convergence.md)).

Recently completed (2026-08-11): **stage-rerun-from-processed** — `--stages` naming only leaf stages (`mesh`, `semantics`, `localize` — derived from `Reconstructor._STAGE_DEPS`, exported as `LEAF_STAGES`) pulls a scene back out of `environments-processed` instead of rebuilding from curated video; anything including `preproc`/`pointcloud` rebuilds, so staleness is unreachable rather than handled. New `collab_splats/remote/rerun.py` (`prepare_scene`, `discover_scenes`); `Reconstructor` gained `_resolve_result()` (loads COLMAP off disk when `self.pointcloud is None`), leaf-stage markers in `_stage_output_exists`, and a refusal when a *named* stage's output already exists without `overwrite`. Nothing deletes a remote object — push stays `rclone copy`. Contract: `configs/README.md` → "Re-running one stage against a processed scene". **Owed: one live GCS run watched by a human** ([spec](docs/superpowers/specs/2026-08-11-stage-rerun-from-processed-design.md) · [plan](docs/superpowers/plans/2026-08-11-stage-rerun-from-processed.md)).

Recently completed (2026-07-30): **gcloud-remote-pipeline** — 12-task GCS remote processing pass: `environments-curated` → reconstruct → `environments-processed/<scene>/` with an `rclone check --one-way` verified push and post-push local cleanup (nothing is deleted until the push verifies); `sparse_pc.ply` now written binary; TSDF output unified on `mesh/mesh.ply` (no legacy `mesh_tsdf.ply` fallback — old scenes need `mesh(overwrite=True)` once); semantics converged on `features.zarr` + `autoencoder.pt` under `<backend>/semantics/<extractor>/`; `SessionSource` → `collab_splats/remote/SceneSource`; new driver `docs/examples/run_pipeline_remote.py` ([spec](docs/superpowers/specs/2026-07-29-gcloud-remote-pipeline-design.md) · [plan](docs/superpowers/plans/2026-07-29-gcloud-remote-pipeline.md)). Processed-scene output contract + known limitations: `configs/README.md`.

Recently completed (2026-07-17): **dashboard-ux-feedback** — 12-task responsiveness/visibility pass: global busy lock, step-level op-log timings, debounced frame preview, all blocking work off the IOLoop, fast-bind server (page up in ~3s, import progress streams to the page) ([spec](docs/superpowers/specs/2026-07-16-dashboard-ux-feedback-design.md) · [plan](docs/superpowers/plans/2026-07-16-dashboard-ux-feedback.md)). **Before committing any dashboard change, run the smoke gate:** `python -m collab_splats.dashboard --smoke` (must print SMOKE PASS). Manual browser checklist (plan Task 12 step 3) still owed. Browsing via IP needs `--websocket-origin`.

Recently completed (2026-07-16): **dashboard-loadtime** — 16-task perf pass (F1-F8, F10-F16; F9 zarr re-chunk deferred to a future decision doc) ([spec](docs/superpowers/specs/2026-07-16-dashboard-loadtime-design.md) · [plan](docs/superpowers/plans/2026-07-16-dashboard-loadtime.md)). Manual browser smoke checklist still owed.

Known test failures: `docs/known-test-failures.md`

## Installation and Setup

```bash
bash setup.sh                    # full install (uv sync — all deps incl. VGGT-X + MapAnything)
```

## Development Environment

- **Python env:** `python` in base shell may be py3.13 (wrong for this project). Always use `/opt/venv/reconstruction/bin/python` (py3.11), or activate the venv: `source /opt/venv/reconstruction/bin/activate`.
- **Memory:** container cgroup cap 46.6 GB. Heavy inference/eval → run in tmux, not notebooks.
- **Side shells:** don't run parallel processes during heavy eval runs (OOM risk).

## Architecture Overview

Core pipeline: video/images → pointcloud (feedforward: VGGT-X / VGGT-Omega / MapAnything / LoGeR, or sfm: InstantSfM + VDA depth) → optional BA / optional LC (both feedforward only — sfm refuses them) → `pointcloud.zarr` → mesh/features/splats.

```
collab_splats/
  pointcloud/              # main reconstruction pipeline
    base.py                # BasePointcloudCreator, PointcloudResult
    feedforward/           # BaseFeedforwardCreator (5-step template method), VGGTXCreator, MapAnythingCreator, FeedforwardResult
    sfm.py                 # InstantSfMCreator (global SfM via upstream python API) + generate_vda_depth (Video-Depth-Anything metric depth)
    utils.py               # lift_features, reproject_pixels, colmap_reconstruction_to_result
    export.py              # write_pointcloud_ply: binary sparse_pc.ply writer
  geometry/                # pose/geometry backend: loop closure + bundle adjustment
    transforms.py          # extrinsics_to_homogeneous, invert_poses, OPENGL_TO_OPENCV (ex utils/geometry.py)
    bundle_adjustment.py   # Levenberg-Marquardt BA
    global_alignment.py    # VGGT-X native feature-match + joint BA (parked; not wired into any creator)
    loop_closure/          # submap pose graph (SL4/SE3), DINO-SALAD retrieval gate, LoopClosure wrapper
  localization/            # camera localization: query image → pose in known reconstruction
    retrieval.py           # Stage 1: BaseRetrievalExtractor, DinoSalad/PECLIP (also used by loop closure)
    extractors.py          # Stage 2: LocalMatcher over the vismatch model zoo (FEATURE_MATCH_MODELS fast paths)
    localizer.py           # Stage 3: CameraLocalizer, zarr feature cache
    viz.py                 # plot_correspondences
  semantics/               # 2D feature extraction
    features/              # BaseFeatureExtractor + RegistryMixin (base.py); registered
                           #   dinov2, maskclip, talk2dino — all ViT-width (384-1024D)
    compression.py         # FeatureAutoencoder: per-point encode/decode + recon_cosine
    segmentation/          # BaseSegmentation; registered insid3, mobilesamv2, sam3
  preproc/                 # video preprocessing: measure (qa) then select (sampling)
    video.py               # ffmpeg/ffprobe decode: get_video_info, iter_frames, extract_frame
    qa.py                  # report-only capture quality: compute_video_quality, load_video_quality
    sampling.py            # sample_fps | sample_uniform | sample_optical_flow + filter_frame_quality
    frame_store.py         # frames.zarr: the decode-once keyframe store
    viz.py                 # sampling analysis plots (notebook-only, not re-exported)
  mesh/                    # TSDF + Poisson meshing (base, poisson, tsdf, utils)
  splats/                  # Gaussian-splat training on upstream gsplat: cameras, losses, rendering, trainer, outputs
  wrapper/                 # stage orchestration: Reconstructor (config-driven pipeline), batch drivers
  remote/                  # rclone/GCS: SceneSource over environments-curated + environments-processed
  dashboard/               # interactive video/scene browser (reads the FLAT dashboard layout only)
  utils/
    torch_utils.py         # RegistryMixin, pytorch_gc, infer_batch_size, batch_iterator, get_device
evals/
  scripts/eval.py          # compute script — CLI/tmux only, never run in notebook
  datasets.py              # dataset loaders (7-Scenes, CO3Dv2)
  results/                 # gitignored
```

## Code Style

- **Imports at top:** all imports at the top of the file — no inline imports inside functions or methods (scripts and notebooks). Exception: optional heavy deps that would break the module on missing install may be imported inside the function that needs them, with a clear `ImportError` message.
- **Inline block comments:** each logical block of code gets a short comment explaining what it does. Comment at block level, not every line. Examples: `# Sort images by filename; reject non-image extensions`, `# Populate BA fields: subsampled world-point grid for track extraction`. Existing comments that meet this standard are kept; missing block comments are added.
- **Section dividers:** use `########`-style dividers to separate major sections in long files (constants, helpers, classes, etc.). Keeps files scannable without opening a doc.
- **Docstrings:** every public function and class gets a one-line summary docstring. The `"""` open and close on their own lines — summary starts on the line after the opening quotes, never on the same line. Multi-line docstrings put a blank line between the summary and the bullets that follow. Bullets, not prose blocks. No restating the function name. No padding.
- **Blank lines between blocks:** a run of statements that does one thing is separated from the next run, and every block comment gets a blank line above it. Walls of undifferentiated code are not human-readable.
- **Don't over-complicate:** prefer the simplest implementation that solves the problem. No premature abstractions, no dead branches for hypothetical future use, no wrapper layers that add no value. If a param is always default, ask whether it should exist.
- `logging` not `print()` — use `logger.debug()` / `logger.info()` throughout module code
- `RegistryMixin` for registry pattern (from `utils/torch_utils.py`)
- Template-method pattern for abstract pipelines (see `BaseFeedforwardCreator`)
- Typed `@dataclass` for pipeline outputs (`FeedforwardResult`, `PointcloudResult`)
- Hard imports — no stub backends; let missing deps raise `ImportError` at import time

## Testing

- Flat test functions — no class-based unless shared fixture state requires it
- `tests/` mirrors `collab_splats/` structure
- Run: `/opt/venv/reconstruction/bin/python -m pytest tests/`

## Evaluation

- `evals/scripts/eval.py` = compute (CLI/tmux only); notebooks in `docs/` = visualization only
- Results: `evals/results/` (gitignored); pass `--submap_size 50` for >100-frame sequences

## Commit Conventions

Conventional commits with scope: `feat(pointcloud):`, `fix(ba):`, `refactor(semantics):`, `docs(decisions):`

## Development Commands

```bash
black . && isort .                                                  # format
/opt/venv/reconstruction/bin/python -m pytest tests/               # test
/opt/venv/reconstruction/bin/python evals/scripts/eval.py --help   # eval
```
